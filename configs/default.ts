export const defaultNegativePrompt =
    "ugly, lowres, tiling, poorly drawn hands, poorly drawn feet, " +
    "poorly drawn face, out of frame, extra limbs, disfigured, deformed, " +
    "body out of frame, bad anatomy, watermark, signature, cut off, " +
    "low contrast, underexposed, overexposed, bad art, beginner, amateur, " +
    "distorted face, blurry, draft, grainy, bad hands, missing fingers, " +
    "extra digit, fewer digits, cropped, worst quality, low quality, text, " +
    "error, normal quality, jpeg artifacts, artist logo, artist name, " +
    "fused clothes, poorly drawn clothes, missing arms, missing legs, " +
    "extra arms, extra legs, extra fingers, duplicate, cloned face, fused fingers, " +
    "long neck, malformed limbs, morbid, mutated hands, mutation, mutilated";

export const defaultNumInferenceSteps: number = 50;
export const defaultWidth: number = 480;
export const defaultHeight: number = 720;
export const defaultGuidanceScale: number = 7.5;
export const defaultRandomSeed: number | string = "";
